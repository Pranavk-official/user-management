// isLoggedIn , isLoggedOut
// isAdmin


module.exports = {
    isLoggedIn: (req,res,next) => {
        if(req.session.user){
            
        }
    },
    isLoggedOut: (req,res,next) => {

    },
    isAdmin: (req,res,next) => {

    },
}